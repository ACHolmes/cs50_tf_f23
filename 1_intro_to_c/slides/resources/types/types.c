#include <cs50.h>
#include <stdbool.h>

int main(void) {
    // Declare variable. MUST include type
    int integer_variable = 1;

    // Update variable, fixed (MUST NOT include type)
    integer_variable = 2;
}