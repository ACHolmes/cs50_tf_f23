#include <stdio.h>

int main (void) {

    // We DECLARE the variable y here. 
    // We can later ASSIGN it value(s)
    int y;

    // We INITIALIZE the variable x here.
    int x = 5;

    // We ASSIGN y a value here.
    y = x * x;

}